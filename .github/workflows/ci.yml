name: Continuous Integration

on:
  push:
    paths-ignore:
      - "**.md"
      - "tools/**"
  # pull_request:
  #   paths-ignore:
  #     - "**.md"
  #     - "tools/**"
  # pull_request_target:
  #   paths-ignore:
  #     - "**.md"
  #     - "tools/**"
  #   types:
  #     - labeled
  # schedule:
  #   - cron: 0 11 * * 1-5

jobs:
  build:
    continue-on-error: ${{ matrix.experimental || false }}
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 14
          # - 16

    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          check-latest: true

      # required because of dilithium key pair class
      - uses: mymindstorm/setup-emsdk@v11

      - name: Install dependencies
        run: npm i

  lint:
    needs:
      - build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          check-latest: true

      - name: Install dependencies
        run: npm run lint

  test:
    needs:
      - build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          check-latest: true

      - name: Install dependencies
        run: npm run test
