import { convertDidToEndpoint } from "./convertDidToEndpoint";
import { convertEndpointToDid } from "./convertEndpointToDid";
import { resolve } from "./resolve";

export { convertDidToEndpoint, convertEndpointToDid, resolve };
export default { convertDidToEndpoint, convertEndpointToDid, resolve };
